# เพื่อจะใช้งาน main.c นี้ แนะนำให้ใช้งานบน Windows 10 ขึ้นไป หรือ Compiler on MacOS ที่รองรับ ฟังข์ชั่นที่มีใน WindowsOS เช่น ใช้งานผ่าน Emulater
!คำเตือน main.c อาจจะไม่รองรับ Code-Block บางเวอร์ชั่น เนื่องจากถูกสร้างขึ้นบน VScode IDE แนะนำให้ใช้งานบน VScode เพื่อประสบการณ์ที่ดีที่สุด

!ข้อควรระวัง!
ไม่ควรแก้ไขข้อมูลหรือลบ config.txt , log_username.txt , log_phone.txt , log_password.txt เนื่องจาก อาจทำให้ระบบมีปัญหาได้
ระบบจะสร้างไฟล์เก็บข้อมูลขึ้นมาเอง ไม่จำเป็นต้องสร้างไฟล์ขึ้นมาให้กับระบบ
แต่ถ้าอยาก ก็แก้ไขก็ได้นะ😏

!setting in code
#define MAX_USER (Number) <to access maximum user accounts in slots>
#define MAX_STRING (Number) <to config how many character can store in string>
มีไว้ตั้งค่าระบบ สามารถแก้ไขผ่าน IDE ได้

Contract DISCORD ID : @kotorisaki (for any information)